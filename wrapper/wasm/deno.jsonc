{
  "tasks": {
    "dev": "deno run --watch main.ts",
    "wasmbuild": "deno run -A https://deno.land/x/wasmbuild@0.15.0/main.ts",
    "dbg": "wasm-pack build -t web && deno test -A"
  },
  "test": {
    "exclude": [
      "demo"
    ]
  }
}
